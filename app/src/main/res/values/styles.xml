<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NavViewMain">
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorControlNormal">@color/textPrimary</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/textPrimary</item>
    </style>

    <style name="Widget.Gitforce.Toolbar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:minHeight">?attr/actionBarSize</item>
        <item name="android:background">@color/colorPrimary</item>
        <item name="elevation">@dimen/dp4</item>
        <item name="android:theme">@style/ThemeOverlay.Gitforce.ToolbarTheme</item>
        <item name="popupTheme">@style/ThemeOverlay.AppCompat.Dark</item>
    </style>

    <style name="ThemeOverlay.Gitforce.ToolbarTheme" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/textPrimary</item>
    </style>

    <style name="TextError">
        <item name="android:textSize">@dimen/sp18</item>
        <item name="android:textColor">@color/grey_600</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
